<div class="row-fluid">
  <h1><%= Service.model_name.human(count: 2) %></h1>
  <%= render partial: 'shared/services', locals: { services: @services, is_deletable: true } %>
</div>

<div class="row-fluid">
  <h1><%= Activity.model_name.human(count: 2) %></h1>
  <%= render partial: 'shared/activities', locals: { activities: @activities } %>
  <p class="more"><%= link_to t('helpers.links.more'), activities_path %></p>
</div>
